# Contributing

Use synthetic certificate subjects and disposable workspaces. Never commit private keys, generated CA state, authentic certificates, personal paths, or signed confidential artifacts.

Changes must preserve Windows PowerShell compatibility, pass `tests/StaticChecks.ps1`, document algorithm claims, and include failure-path testing.
