# Security policy

Use GitHub private vulnerability reporting. Provide sanitized reproduction steps and disposable test material. Never attach a real CA private key, signer key, passphrase, production certificate database, or confidential signed artifact.

The latest tagged release is supported. Local provider builds and custom OpenSSL forks are evaluated on a best-effort basis.
